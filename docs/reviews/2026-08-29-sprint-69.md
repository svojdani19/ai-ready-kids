# Sprint 69 — archiving was bookkeeping, not a boundary

**Date:** 2026-08-29
**Reviewed against:** HEAD `6ae0159`
**Scope:** make `archived_at` an authorization boundary and revoke the class
credential on the transition. No student field, no free text, no telemetry, no
risk scoring, no new auth system, no schema change.

## The defect

Archiving dropped a cohort out of the active licensed-seat count and refused new
code entry at both `/join` steps. It did nothing to a student session already
issued.

`currentStudent` loaded the student, loaded the class, compared the session's
bound code — sprint 68's fix — and handed back an **archived** classroom. Every
student page and every instructional action trusts that resolver, so a child
with a live twelve-hour cookie kept the roster-linked experience and kept
opening and recording authored mission and check-in work after the class was
"finished". Both archive paths were affected: `archiveClassAction` and
`rolloverYearAction`, which archives the whole outgoing year.

Commercially that is a school archiving a cohort to free licensed seats while
already-signed-in devices carry on using the instructional product.
Operationally, "finished class" and year rollover did not close student access.

## The correction

**One line in the resolver, one transaction in the repository.**

`currentStudent` refuses an archived class — closing every student page and
mutation on the next request, since they all go through it.

That alone leaves a second hole: restore the class inside those twelve hours and
every pre-archive session revives, because the code it is bound to is the code
the class still has. So `archiveClass` issues a **new join code in the same
transaction** as the archive:

```ts
if (current && !current.archived_at) {
  db.prepare("UPDATE classes SET archived_at = ?, join_code = ? WHERE id = ?")
    .run(nowIso(), generateJoinCode(db), id);
}
```

No schema field — the existing credential lifecycle does the revocation, exactly
as code rotation already does. Sessions and grants are bound to the old code and
stay invalid **across a restore**; everyone rejoins with the code the class has
now.

- **Both paths covered**, because both go through this one function. The manual
  archive and the rollover are not two implementations.
- **Idempotent.** Archiving an already-archived class touches nothing: no second
  code, no moved timestamp. The read and the write share one `BEGIN IMMEDIATE`
  so two archives arriving together cannot both see an active class and both
  rotate.
- **Unknown class is a no-op**, as before.
- **Staff sessions unaffected** — a teacher must still be able to restore,
  rename or delete an archived class.
- **Restore's seat cap and the subscription rules are untouched.**

## Evidence

### Failing-before, behaviourally through the real resolver

`tests/archive-boundary.test.ts` drives `currentStudent` with a mocked cookie
jar and `globalThis.__airkDb` on a seeded fixture. Against `6ae0159`, **6 of 11**
behaviour tests fail:

```
× rejects a student session on the next request after archive
× stays rejected after the class is restored
× issues one new code, and archiving again changes nothing
× rotates once through the rollover path too
× preserves every record row for row, apart from archived_at and join_code
× refuses the real student actions after archive, and writes nothing
```

The first is the defect exactly: a valid student session stayed valid after
archive.

### Acceptance coverage

| Requirement | Test |
| --- | --- |
| valid before archive, null after, through `currentStudent` | `rejects a student session on the next request after archive` |
| pre-archive session rejected and **stays** rejected after restore | `stays rejected after the class is restored` |
| new join with the current code works after restore | `lets a child back in with the code the class has now` |
| archiving another class does not invalidate this student | `does not touch a student in a different class` |
| staff unaffected | `leaves staff sessions alone` |
| manual and rollover rotate exactly once; no-op is stable | `issues one new code, and archiving again changes nothing`, `rotates once through the rollover path too`, `is a no-op for a class that does not exist` |
| records preserved row for row; seats fall | `preserves every record row for row, apart from archived_at and join_code`, `frees the licensed seats the roster was using` |
| the real student Server Actions refuse after archive and write nothing | `refuses the real student actions after archive, and writes nothing` — **corrected during acceptance, see below** |
| copy matches behaviour | four tests in `the copy says what archiving now does` |

The preservation test compares JSON dumps of `students`, `attempts`,
`benchmarks`, `assignments`, `users`, `schools` and every class column except
`archived_at` and `join_code` — so any other drift fails.

```
typecheck  ✓
lint       0 errors, 2 pre-existing warnings
tests      733 passed (20 files)   — up from 718
build      ✓ Compiled successfully
```

### Copy, traced and aligned

Four surfaces, each with a failing-before copy test:

| Surface | Now says |
| --- | --- |
| Archive confirmation | *"…any child already signed in is asked to rejoin next time they load a page. The class gets a new join code, so the old one will not work even if you restore it… The roster, mission history, check-ins and the deletion date do not change."* |
| Classes page description | *"Archiving closes a finished class: students cannot join and those already signed in are signed out on their next request, while its records and their deletion date are unchanged."* |
| `class.archived` audit | *"…those already signed in are rejected on their next request; the class was issued a new join code, so the old one stays invalid even if the class is restored."* |
| Rollover preview, result and audit | *"Each newly archived class gets a new join code, and children signed in to it are asked to rejoin on their next request. The old codes and sessions stay invalid even if you restore a class later."* |

A test forbids `immediately`, `straight away` and `signed out at once` in the
confirmation: nothing implies a rendered page is erased. Extraction is anchored
with no fallback and requires exactly one match — sprint 68's lesson applied at
the point of writing rather than after review.

### Browser

**A single browser profile holds one `airk_session` cookie**, so the student and
administrator sessions cannot be live in it at the same time. The two halves were
therefore driven as separate passes, and are recorded separately. Both halves
were run at **both** widths.

**Student device — 1280×800, then 768×1024.** Joined with the current code, chose
a child, `/student` rendered their name, missions and badge strip. The class was
then archived through the same `archiveClass` transaction the administrator
action performs. Navigating to `/student`:

```
url /join   serverError false   stale.name false
            overflow    false   stale.missions false
            go.unobscured true  stale.badges false
```

The class was then **restored** with the old cookie still in the browser:
`/student` still landed on `/join` — `reactivated: false` — proving a restore
does not revive a pre-archive session. Typing the **old** code was refused
(*"That code did not match a class"*), and typing the **new** code returned the
child to `/join/cls_room12`.

**Administrator — 768×1024, then 1280×800.** The real UI throughout. Signed in
as the administrator, opened `/admin/classes`, pressed **Archive** on Room 12
(control `visible / inViewport / unobscured` by `elementFromPoint`), read the
corrected confirmation in full, pressed **Archive class**. The row became
`Room 12 Archived` and the code changed — `COMET-UMBRELLA-604` on the tablet
pass, `COMET-JACKET-540` on the desktop pass — with no server error and no
document overflow (the wide table scrolls inside its own `overflow-x: auto`
container). `/admin/data` showed the corrected audit entry. **Restore** was then
driven at both widths: the row returned to active and the code correctly stayed
rotated.

Demo restored exactly afterwards: Room 12 `MAPLE-HERON-317` and unarchived, the
test `class.archived` / `class.restored` audit rows removed, and counts
re-confirmed — 4 classes, 90 students, 884 attempts, 53 assignments, 134
benchmarks, 6 audit rows, with the audit list back to its seeded six.

## Correction during acceptance — the "real mutation" test called no action

As first written, that test was:

```ts
const resolved = await currentStudent();
expect(resolved).toBeNull();
if (resolved) {
  startAttempt(...); recordDecision(...); completeAttempt(...);
}
```

On the corrected implementation the branch is **unreachable**, so no exported
action ran. The test proved `currentStudent` returns null a second time and
nothing whatsoever about `beginMission`, `submitDecision`, `finishMission` or
`submitCheckInAnswer`. The acceptance table above claimed "a real mutation after
archive writes nothing", which the test did not establish. It also reached for
the repository directly, which is not the door a child's browser knocks on.

### What it does now

Three exported Server Actions, called directly, with **no resolver check in
front of them** and no duplication of the resolver's logic:

- **`submitDecision`** with a genuinely reachable scene and choice — an assigned
  mission the child has not finished, walked to its live decision point via
  `expectedDecisionSceneId`, so the call would append to `path_json` and merge
  `evidence_json` on the old code. Its refusal is returned, not redirected:
  `{ ok: false, error: "That mission is not open for your class." }`.
- **`submitCheckInAnswer`** with the window opened to `pre` and the child's
  existing benchmark rows cleared, so the call would write on the old code:
  `{ ok: false, error: "That check-in is not open." }`.
- **`beginMission`**, which returns nothing, so its refusal is the redirect
  `requireStudent` performs: `rejects.toThrow(/REDIRECT:\/join/)`.

Then the `attempts` and `benchmarks` rows for that student are compared **byte
for byte** — full row JSON, so `path_json`, `evidence_json` and `completed_at`
are all in scope.

`next/navigation` and `next/cache` are mocked so a redirect is a recognisable
error rather than a framework throw, which lets the test distinguish "the action
refused" from "the action blew up".

### It detects the original hole

Checking `src/lib/auth/session.ts` and `src/lib/repo/classroom.ts` out at
`6ae0159` and re-running:

```
× refuses the real student actions after archive, and writes nothing
AssertionError: expected { ok: true } to deeply equal { ok: false, … }
```

`submitDecision` returns **`{ ok: true }`** on the pre-fix code — the action was
accepted and the decision written, with the class archived. That is the hole,
caught by the action rather than inferred from the resolver.

The direct repository mutation imports (`startAttempt` for setup aside,
`recordDecision` and `completeAttempt`) are gone; lint reports no unused import.

Gate re-run for the test change: `typecheck ✓`, `lint 0 errors / 2 pre-existing
warnings`, **733 tests passed (20 files)**, `build ✓`. `git diff --stat src/` is
empty — **no production change**, and the browser evidence above is unaffected.

## Remaining gaps

- **Containment is per request.** A page already rendered on a child's screen
  stays rendered until they navigate. Nothing pushes to a connected browser, and
  the copy says so rather than implying otherwise.
- **Revocation is code rotation, not session invalidation.** There is no session
  store to revoke against, so "invalidate this child's session" is only
  expressible as "change the credential it was bound to". That is enough for
  archive and rotation; it could not express, say, signing out one child while
  the class carries on.
- **The seat-count consequence is now real but still coarse.** Archiving frees
  seats *and* closes access together. A school wanting one without the other has
  no way to ask for it, which is the right default and worth naming.
- **A restore always costs a rejoin.** Archiving by mistake and restoring
  immediately still sends every child back to the join screen with a new code.
  That is the price of not letting a restore revive sessions, and the
  confirmation now says so before the administrator commits.
