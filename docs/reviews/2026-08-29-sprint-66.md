# Sprint 66 — risk conclusions the product could not support, and two inventories that were not the schema

**Date:** 2026-08-29
**Reviewed against:** HEAD `f9dc70c`
**Scope:** buyer-facing privacy and security copy, and the student data
inventory. No authentication change, no new student field, no telemetry, no
per-student admin view.

## 1. Three surfaces drew a risk conclusion instead of describing a control

| Surface | Claim |
| --- | --- |
| Public home | *"A first name and a last initial is the entire student record. Nothing to reset, nothing worth stealing."* |
| Admin → Classes | *"…which is exactly as much security as this data warrants."* |
| Public privacy | *"…the security is proportionate to that."* |

Each is a conclusion about risk, not a statement about what the software does,
and each sat on the same product that says elsewhere — accurately — that mission
and check-in records are **education records under FERPA**. A page cannot call
something an education record and dismiss access to it in the same breath.

They were also wrong about what the code reaches. Verified in the code, not
inferred:

- `src/app/join/[classId]/page.tsx` renders **every child in the class by name
  and avatar** to anyone holding a valid grant for that class.
- `src/app/actions/auth.ts` → `chooseStudent` writes a **student session** for
  any listed child, which opens that child's badges, finished missions and
  per-skill evidence.

So the code-holder does not merely reach "a child's own progress list". They
reach the roster, pick from it, and become the child they picked. A code is read
aloud to a room of seven year olds, who cannot meaningfully distinguish a shared
classroom credential from a personal one — which is exactly why the limitation
has to be described rather than reasoned away.

### The correction

`src/content/data-inventory.ts` → `CLASS_CODE_BOUNDARY`, said the same way
wherever codes are explained:

> A class code is shared classroom access, not proof of who is using it. It is
> typed by a room, not held by a person. · Anyone with the code can see that
> class's roster of first names and avatars, choose any child listed on it, and
> open that child's progress. · It reaches one class. It is not a way into
> another class, into a teacher's tools or into anything an administrator sees.
> · Rotate the code whenever it has travelled further than the class. The old
> code stops working immediately and nothing else about the class changes.

The third line is a real scope limit the product does enforce (grant is
per-class; `chooseStudent` refuses a student from another class), so it belongs
in a description of the boundary. It is not offered as a reason the first two
lines do not matter.

`CLASS_CODE_POSTURE` says what the build is and who decides the rest
(**corrected during acceptance — see below**):

> Roster sync and single sign-on would replace class codes in a production
> deployment. Neither is built here, so a class code is the only credential in
> the product, and it is **not production access control**. What this build is,
> is a **local demonstration running fictional data**. Before any pilot that
> puts **real student records** behind a class code, **the school has to weigh**
> the shared-access limitation above against its own policies and decide what
> operational controls it requires. That **decision belongs to the school**, and
> nothing here is a **vendor assurance** that a class code meets its bar.

The home page card is now *"A class code, not a child's password"*. The existing
accurate statements — no password to reset, no recovery flow to attack, roster
sync and SSO not built — are kept.

## 2. Two "complete" inventories, neither of which was the schema

`/admin/data` listed six items under *"The complete list. There is nothing held
back."* `/privacy` listed a **different** six under *"Everything we hold about a
student."* Counting the columns of the three student-linked tables —
`students` (5), `attempts` (7), `benchmarks` (6) = **18**:

- the admin list accounted for **4 of 18**;
- the public list accounted for **7 of 18**.

Omitted by both: `attempts.evidence_json` (the derived per-skill
demonstrated/developing judgement), `benchmarks.started_at` /
`completed_at`, `students.created_at`, and every id and foreign key. Omitted by
the admin list alone: `students.class_id` — the class relationship, which the
public page did name. Two absolute claims, disagreeing with each other and with
`schema.ts`.

### The correction

One module both pages read: `src/content/data-inventory.ts`. Eleven entries,
each naming the `table.column` values it accounts for, and
`tests/data-inventory.test.ts` fails when any column of any student-linked table
is unclaimed — so a future `ALTER TABLE` breaks the build until somebody says
out loud what the new column is. That is what makes "every" checkable rather
than aspirational.

Scope is stated instead of assumed:

- **Admin:** *"Every column in the database that hangs off a student row, and
  the class and staff records those rows hang from. Not the school's own account
  settings, which are not student records."* The class/assignment/staff rows the
  old list already carried are kept, in their own labelled group.
- **Public:** *"That is every column in the database that hangs off a student
  row… It is not everything the product stores: a school's own account settings,
  its staff records and the audit log are not records about a child, and this
  list does not cover them."*

The public list stays student-scoped. Neither heading was expanded into a claim
about all product data, so neither had to grow to cover it.

## Evidence

```
typecheck  ✓
lint       0 errors, 2 pre-existing warnings
tests      690 passed (17 files)   — up from 678
build      ✓ Compiled successfully
```

**Failing-before:** reverting the four page files (keeping the new module, which
the test imports) fails **6 of 12** new tests — every copy and cross-surface
assertion:

```
× keeps the inventory to student scope on both surfaces
× is one list read by both surfaces, not two lists that agree today
× home draws none of them
× privacy draws none of them
× adminClasses draws none of them
× describes what a class code actually reaches, everywhere it is explained
```

The other six are the schema-coverage assertions. **They cannot fail-before, and
that is worth saying plainly:** before this sprint no list was schema-linked at
all, so there was nothing for them to check. They are forward guards against the
omission recurring, not evidence that it existed — the counts above (4/18 and
7/18) are that evidence.

Browser, dev server on :3210, four surfaces × two viewports:

| Surface | 1280×800 | 768×1024 |
| --- | --- | --- |
| `/` (home) | ✓ | ✓ |
| `/privacy` | ✓ | ✓ |
| `/admin/data` | ✓ | ✓ |
| `/admin/classes` | ✓ | ✓ |

Every combination: banned phrases absent, boundary and posture sentences
present, the previously-omitted facts (derived evidence, check-in timestamps,
class relationship) rendered, and `scrollWidth === clientWidth`.

## Correction during acceptance — two claims still ahead of the evidence

### 1. The replacement posture was the same conclusion, drawn narrower

My first version said a class code was *"enough for a **supervised pilot** or a
local demonstration, where an **adult is in the room** and the roster is
fictional or **small and known**"*. Every clause in that is a risk judgement I
had no basis for, and the two mitigations do not mitigate:

- An adult in the room does not stop a code read aloud or photographed off a
  whiteboard from being used that evening, from anywhere.
- A small or familiar roster does not stop the holder from choosing a different
  child on it — which is precisely the reach the sprint had just documented.

Having removed *"nothing worth stealing"* for being a conclusion rather than a
control, I wrote a conclusion rather than a control on the line underneath.

Corrected: **a local demonstration on fictional data** is the only posture named,
and the real-student question is handed to the school as the school's judgement.
No vendor assurance in either direction, and *"not production access control"*
is kept. The admin Classes note was aligned to the same words.

### 2. `students.created_at` was given a purpose it does not have

The entry said the column existed *"so an administrator can see when a record
entered the system"*. It does not. Traced through the code:

- Written by the `INSERT` in `src/lib/repo/classroom.ts` and read back only by
  `SELECT *`.
- **No route renders it.** The only `.created_at` rendered anywhere under
  `src/app/admin`, `src/app/teacher` or `src/app/student` is `entry.created_at`
  on the audit log, which is a staff action, not a child.
- **Nothing computes from it.** `retentionRows` and `runScheduledPurge` work
  from the class's own `year_ends_on`; `buildSchoolReport` never touches it.

An inventory that invents a benefit for an unused column is describing a product
that does not exist — the same failure as the two lists it replaced, one entry
deep. Corrected to the boundary: *"Database metadata written when the row is
inserted. No screen in this product displays it, and nothing computes from it…
It is listed because it is in the database, not because it does anything."*

The column is **kept**. Dropping it means a schema change and a migration for
existing databases, which is more risk than an unused `TEXT` column carries, and
larger than this correction's scope.

### Evidence for the correction

Two focused tests, plus one that checks the code rather than the copy:

- *"names no deployment this build is judged good enough for"* — requires
  `local demonstration running fictional data`, the school-judgement and
  vendor-assurance clauses; forbids `enough`, `suitable`, `approved`, `safe`,
  `proportionate`, `supervised pilot`, `fine for`, `acceptable`. The
  surface-wide banned list also gained `supervised pilot`, `adult is in the
  room` and `small and known`.
- *"claims no visibility or purpose for a column nothing reads"* — forbids
  `so an administrator can see` and any `shown to`/`displayed to`/`visible to`;
  requires the three factual clauses.
- *"proves that column is rendered nowhere, rather than asserting it"* — walks
  every `.ts`/`.tsx` under the three routed roots and fails on any `created_at`
  reference other than the audit entry. A copy assertion about invisibility is
  only as good as the code, so this checks the code.

Against `b56aeb2`, **5 of 15 fail**:

```
× names the facts the two lists used to omit between them
× privacy draws none of them
× adminClasses draws none of them
× names no deployment this build is judged good enough for
× claims no visibility or purpose for a column nothing reads
```

Re-verified: `typecheck ✓`, `lint 0 errors / 2 pre-existing warnings`,
**693 tests passed (17 files)**, `build ✓`. `/privacy`, `/admin/data` and
`/admin/classes` rechecked at 1280×800 and 768×1024: banned phrases absent,
`local demonstration` + `fictional data` present, school-judgement clause
present, `not production access control` present, no horizontal overflow.

## What was deliberately not done

- **No authentication change.** Class codes remain the only student credential.
  This sprint made the product describe that accurately; it did not fix it, and
  a fix is roster sync or SSO, which is a build, not a copy edit.
- **No new field, telemetry, per-student admin view, risk score, open text or
  auth.** The grades 2–4 authored niche is untouched.
- **The commercial limitation stands and is now stated on the page a buyer
  reads:** an administrator evaluating a paid pilot is being told, in the
  product, that anyone with a class code can open any listed child's progress,
  that this build is a local demonstration on fictional data, and that putting
  real student records behind a class code is their decision to weigh — not a
  posture the vendor has cleared. That is the honest position, and it will cost
  some deals. It is better than the alternative, which was a security claim the
  code did not support.
