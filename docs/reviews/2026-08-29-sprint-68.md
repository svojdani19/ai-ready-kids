# Sprint 68 — rotation invalidated the way in, not the people already inside

**Date:** 2026-08-29
**Reviewed against:** HEAD `53a5d6e`
**Scope:** bind a student session to the code that issued it, and align every
rotation claim. No database field, no student data field, no telemetry, no risk
scoring, no free text, no broader auth platform.

## The defect

Rotation is the recovery action the product recommends by name. Sprint 66 made
that recommendation more prominent, on the reasoning that a code read aloud to a
room will travel and rotation is the answer when it does.

It was half an answer.

| Credential | Before rotation | After rotation |
| --- | --- | --- |
| A new join with the old code | works | **rejected** — `getClassByJoinCode` no longer matches it |
| A half-finished join grant | works | **rejected** — the grant carries the code, and `/join/[classId]` and `chooseStudent` both re-check it |
| **A student session already issued** | works | **still works** |

The session was `{ kind: "student", studentId }`. It recorded *who* was signed
in and nothing about *how they got in*, so `currentStudent` had nothing to
compare against the class's current code — it loaded the student, loaded the
class, and returned both. A browser that entered through a leaked code stayed
inside the class for the rest of the twelve-hour session lifetime, after the
administrator had done exactly what the product told them to do.

Meanwhile three surfaces and two audit entries said *"the old code stops working
immediately"*.

## The correction

**One field, in the cookie, nowhere else.**

`SessionValue` for a student is now
`{ kind: "student"; studentId: string; code: string }` — the normalised join
code, which is the same marker the join grant already carried. It is not
persisted: it lives inside the signed HttpOnly cookie, no screen renders it, and
no column was added.

- **`chooseStudent`** issues the session with `grant.code` — the code the
  verified grant carries, which the checks immediately above it have just
  confirmed is still the class's current one. It is not re-read from the class:
  a session should record what authorised it.
- **`enterDemo("student")`** reads the demo student's class and issues against
  its current code, so a demo session is no more privileged than a real one.
- **`currentStudent`** compares `normaliseJoinCode(classroom.join_code)` with
  `normaliseJoinCode(session.code)` and returns `null` on mismatch. Both sides
  are normalised so a stored code that was never normalised cannot fail to match
  itself. Archive and entitlement checks are untouched, as is the twelve-hour
  maximum lifetime.
- **`currentStaff`** is unchanged. A teacher's session has nothing to do with a
  class code, and rotating a code must not sign out the person rotating it.

**Legacy tokens fail closed.** `decodeSession` requires a non-empty `code` on a
student token. A token from an older build has none and decodes to `null`.
Treating a missing binding as "matches any code" would have preserved the exact
hole being closed. The one-time consequence, stated rather than smoothed over:
**student sessions issued before this commit are rejected, and those children
are asked to rejoin once.** There is no seamless migration and none is claimed.

## What rotation still cannot do

A page already rendered on a screen stays rendered. Nothing in this build pushes
to a connected browser, so a child mid-mission keeps that screen until they
navigate — at which point the next request is rejected. The copy now says this
rather than implying real-time control:

> From then on the old code is rejected on the next request that uses it:
> nobody can join with it, a half-finished join stops, and a browser already
> signed in with it is asked to rejoin the next time it loads a page. …
> What rotation cannot do is reach into a page already on a screen.

Aligned in five places: `CLASS_CODE_BOUNDARY` (rendered on `/privacy` and the
home page), the admin Classes note, both `class.code_rotated` audit details
(admin and teacher actions), and the README's access-control section — which now
also records the binding, the comparison point and the legacy-token consequence.

## Evidence

### Failing-before, behaviourally

`tests/session-binding.test.ts` drives the real `currentStudent` path with a
mocked cookie jar and `globalThis.__airkDb` pointed at a seeded fixture — not a
token-shape assertion and not a helper. Removing only the comparison line from
`currentStudent`, keeping everything else:

```
× is valid before its own class code rotates, and invalid after
× rejects a session bound to a code that was never this class's
  Tests  2 failed | 5 passed (7)
```

The first is the defect exactly: `currentStudent()` returns the student and
their classroom after the class code has been rotated.

### Acceptance coverage

| Requirement | Test |
| --- | --- |
| encode/decode round-trip covers the binding | `round-trips a staff and a student session` |
| legacy session rejected | `rejects a legacy student session that carries no code binding` (token shape) and `rejects a legacy session token that carries no binding at all` (through `currentStudent`, signed with the fixture's real key) |
| valid before rotation, invalid after | `is valid before its own class code rotates, and invalid after` |
| rotating another class does not invalidate | `survives a rotation of a different class` |
| staff unaffected | `leaves staff sessions alone, before and after a rotation` |
| join-grant invalidation still covered | the existing grant tests in `access-control.test.ts`, unchanged |
| copy matches behaviour | `promises only the rotation containment the code delivers`, `says the same thing in the administrator's note and the audit entry` |

```
typecheck  ✓
lint       0 errors, 2 pre-existing warnings
tests      718 passed (19 files)   — up from 706
build      ✓ Compiled successfully
```

### Browser

Each flow is recorded at the width it was actually driven at. Nothing below is
claimed for a width it was not exercised at.

**Student join → rotate → recover, at 1280×800.**

1. `/join` → typed `MAPLE-HERON-317` → roster at `/join/cls_room12` → tapped
   *"I am Aisha K."* → `/student` rendered her missions and badges.
2. The class code was rotated to `PINE-BADGER-451` underneath that live session.
3. Reloading `/student` landed on **`/join`** — no 500, and no stale authorised
   data (`/Aisha|Hello,|badges/` absent from the page).

**Student join → rotate → recover, at 768×1024** — run separately during
acceptance, because the first pass had exercised the flow only at desktop and
checked the tablet layout for copy alone. That did not prove the recovery path
on the Chromebook/tablet layout, which is where these children actually are.

1. Viewport emulated at 768×1024, session cleared, `/join` → typed
   `MAPLE-HERON-317` → roster at `/join/cls_room12` → tapped a listed child →
   `/student` rendered *"Hello, Elias"*, 16 of 18 missions in Room 12, and the
   badge strip.
2. Room 12 rotated to `PINE-BADGER-451` underneath that live session.
3. Navigating to `/student` landed on **`/join`**:

   ```
   url            /join            serverError   false
   stale.name     false            overflow      false
   stale.missions false            go.visible    true
   stale.badges   false            go.inViewport true
                                   go.unobscured true
   ```

   `go.unobscured` is `document.elementFromPoint` at the button's centre
   returning the button itself, so the primary action is not merely rendered but
   actually hittable.
4. The rejoin screen was then **used**: typing the new code at that width
   returned the child to `/join/cls_room12`. Recovery is complete on the tablet
   layout, not just survivable.

**Administrator rotation, at 1280×800.** Signed in as the administrator, opened
`/admin/classes`, pressed **New code** on Room 4 and confirmed. The code changed
to `ELBOW-OCTOPUS-688` and the audit entry on `/admin/data` read: *"Room 4 has a
new class code. The old one is now rejected on its next use, for new joins and
for browsers already signed in with it."*

**Administrator rotation, at 768×1024** — driven separately during acceptance,
and it **found a defect**. See *"The defect the tablet administrator run
exposed"* below.

1. Signed in through the administrator demo at 768×1024, opened
   `/admin/classes`. Room 4 showed `ACORN-BADGER-208`, four **New code**
   buttons, and no document-level horizontal overflow — the wide table sits in
   its own `overflow-x: auto` container (`wrapperOverflowX: "auto"`,
   `scrollable: true`), which is the page pattern rather than a clipped layout.
2. Pressed **New code** on Room 4. The confirmation panel rendered inline and in
   full at that width:

   ```
   confirm  { visible: true, inViewport: true, unobscured: true }
   cancel   { visible: true, inViewport: true, unobscured: true }
   docOverflow false
   ```

   `unobscured` is `document.elementFromPoint` at each control's centre
   returning that control, so both are hittable and not merely painted.
3. Pressed **Change the code**. Room 4's code visibly changed in the table to
   `DRAGON-ASPEN-818`, the old code was gone from the page, `serverError` false,
   `docOverflow` false.
4. `/admin/data` at the same width showed: *"Room 4 has a new class code. The old
   one is now rejected on its next use, for new joins and for browsers already
   signed in with it. The roster and all student records are unchanged."*

Room 4 was restored to `ACORN-BADGER-208` and the one test `class.code_rotated`
audit row deleted. Counts confirmed unchanged: 4 classes, 90 students, 884
attempts, 53 assignments, 134 benchmarks, 6 audit rows, 0 assignment notes.

**Copy, at both 1280×800 and 768×1024** on `/admin/classes`, `/privacy` and
`/admin/data`: the rejected-on-next-request wording present, the
page-already-open limit present, `stops working immediately` absent, no
horizontal overflow. Copy-only checks are recorded as such; they are not
evidence that an interaction on the same page works at that width, which is the
distinction the section above now keeps.

### The defect the tablet administrator run exposed

Opening the confirmation dialog — rather than reading the source for it —
surfaced two rotation claims this sprint's own alignment sweep had missed:

| Where | What it said |
| --- | --- |
| `admin/classes/page.tsx` confirm question | *"The old one and anybody halfway through joining with it **stop working straight away**."* |
| `teacher/class/[classId]/page.tsx` confirm question | *"Everybody will need the new code, including anybody halfway through joining."* |

Two failures in one. Both are the **immediacy claim under different words**, so
my grep for `stops working immediately` walked straight past them. And both name
only the half-finished join — omitting the student sessions that are the entire
subject of this sprint, which made the administrator's last screen before
confirming the least accurate description of what confirming does.

Corrected to the same wording as the note and the audit entry:

> Give Room 4 a new code? From then on the old code is rejected on its next use:
> nobody can join with it, anybody halfway through joining stops, and a student
> already signed in with it is asked to rejoin next time they load a page. The
> class, its teacher, the roster, assignments, mission history, badges and both
> check-ins are not touched.

`tests/data-inventory.test.ts` → *"aligns the rotation confirmations a staff
member actually reads"* reads both confirm questions, forbids
`stop working straight away` and `stops working immediately`, and requires both
`already signed in with it` and `rejoin next time they load a page`. It fails
against the pre-correction files.

Re-run after the fix: `typecheck ✓`, `lint 0 errors / 2 pre-existing warnings`,
**718 tests passed (19 files)**, `build ✓`. The corrected prompt was then driven
through to completion at 768×1024, which is the run recorded above.

#### That test did not read what it said it read

First written as:

```ts
const question = copy.match(/question=\{?"?`?([^`"}]*new code[^`"}]*)/i)?.[1] ?? copy;
```

`[^`"}]*` stops at the `}` of `${classroom.name}`, so it never reached *"a new
code"* on the admin page and **fell back to the entire file**. Evaluated
directly: `admin/classes/page.tsx` → `FALLBACK (whole file)`,
`teacher/class/[classId]/page.tsx` → matched. Every assertion about the
administrator's confirmation was running against the whole source, where the
explanatory note under the table legitimately contains *"already signed in with
it"*. The claim in the paragraph above — that the test reads both confirm
questions — was not true of one of them.

**Demonstrated, not argued.** Changing the admin question's *"a student already
signed in with it is asked to rejoin next time they load a page"* to *"a student
using the old one is asked to rejoin next time they load a page"* — a real
regression, no banned phrase, note left accurate:

| | verdict |
| --- | --- |
| Old expression | `FALLBACK (whole file)`, banned false, sessions true, rejoin true → **PASSES** |
| Corrected test | **FAILS** — `expected 'Give Room 4 a new code? …' to match /already signed in with…/` |

The reviewer's own requested case — restoring the whole pre-correction admin
question with the note left in place — also fails the corrected test, on the
banned-phrase assertion.

**The correction.** `rotationQuestion(path, pattern)` extracts one question and
has **no fallback**:

- The admin pattern anchors to
  ``question={`Give ${classroom.name} a new code?…`}`` specifically, so the
  Archive and Delete-data `ConfirmAction`s on the same page cannot be read
  instead. `${classroom.name}` is substituted with a real class name so the
  assertions read the sentence a staff member sees.
- The teacher pattern anchors to its own `Everybody will need the new code…`
  string.
- `matchAll` plus `toHaveLength(1)` means zero matches or two both fail: a
  renamed prop, a malformed template or a second rotation confirmation is an
  error, never a widened search.
- Each extraction is then asserted to start with its own question, and the
  untouched-records clause is asserted on the **admin question**, not the page.
- A separate test, *"fails rather than widening when a confirmation cannot be
  extracted"*, asserts the helper throws on a pattern that matches nothing.

Gate re-run for the test change: `typecheck ✓`, `lint 0 errors / 2 pre-existing
warnings`, **718 tests passed (19 files)**, `build ✓`. No production file
changed — the corrected test exposed none.

Demo restored after each run: Room 12 `MAPLE-HERON-317`, Room 4
`ACORN-BADGER-208`, every `class.code_rotated` audit row removed — 4 classes /
90 students / 884 attempts / 53 assignments / 6 audit rows / 0 assignment notes.

The tablet **student** run exposed no defect. The tablet **administrator** run
did, so two confirm questions changed and the gate was re-run — the figures in
this review are the post-fix ones.

## Remaining production gaps

- **No revocation of a rendered page.** Containment is per request. A deployment
  that needed a child's screen to clear on rotation would need a push channel,
  which this build does not have and which the copy no longer implies.
- **The binding is a code, not a version counter.** Rotating a code twice back to
  the same value — vanishingly unlikely with this generator, but not impossible
  — would revalidate an old session. A monotonic per-class version column would
  close that, and is a database field, which this sprint was told not to add.
- **Sessions still last twelve hours** and are still the only student credential.
  Rotation now bounds a leak to "until the next request", which is a real
  improvement and is not the same as short-lived sessions or SSO.
- **The one-time rejoin is real.** Anyone with a student session from before this
  commit is signed out. In a local demonstration that costs nothing; in a
  deployment it would need saying to teachers in advance.
